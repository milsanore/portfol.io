# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Logging
LOG_LEVEL=info

# Schema validation
# Enable response schema validation against src/schema.json.
# Recommended: true for integration tests, false in production (performance).
ENABLE_RESPONSE_VALIDATION=false
