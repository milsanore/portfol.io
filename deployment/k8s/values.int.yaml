replicaCount: 2

env:
  NODE_ENV: production
  LOG_LEVEL: info
  ENABLE_RESPONSE_VALIDATION: "true"
  DATABASE_URL: postgresql://postgres:postgres@postgres-int.example.internal:5432/portfolio

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
