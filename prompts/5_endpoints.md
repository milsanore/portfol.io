# INTRO
- endpoints


# CODE
decimal.js
static schema - openapi.yaml

# THE DATABASE
- we will use the latest version of postgres as the backing store
- we will use JSONB storage, so that we don't have to deal with an ORM
- we need a migration library that allows us to write pure SQL
  - if you have no better library, i suggest *******************
- create a `migrations` folder at the root of the project

## TABLES


## KAFKA



